import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "SoSo Analyst — Institutional Research for On-Chain Finance",
  description: "AI-powered crypto research agency built on SoSoValue Terminal. Daily briefs, asset deep dives, and theme reports with SoDEX execution.",
  keywords: ["crypto research", "SoSoValue", "SoDEX", "DeFi", "ETF flows", "on-chain finance"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
